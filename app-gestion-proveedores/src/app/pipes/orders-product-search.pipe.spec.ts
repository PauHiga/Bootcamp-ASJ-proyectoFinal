import { OrdersProductSearchPipe } from './orders-product-search.pipe';

describe('OrdersProductSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdersProductSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
